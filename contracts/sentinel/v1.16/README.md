# Sentinel contract package v1.16

Adds read-only `AuditQuery`/`AuditPage` analytics contracts and the bounded audit
route. World, stream, interactive, scenario, observed-history and external module
wire versions are unchanged from v1.15. No SQLite migration or recording rewrite.
Historical packages and frozen external contracts remain intact.
