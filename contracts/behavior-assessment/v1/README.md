# Behavior assessment contract v1

The request is an immutable snapshot produced by the Rust authority. The
Python worker owns no mission history. A response is accepted only if Rust
confirms its request identity, mission epoch, track revision, feature-contract
version, model allowlist and expiry against current authoritative state.

Assessments are decision-support evidence. They are not commands, group
identity, hostility, engagement authority or provider outcomes.

