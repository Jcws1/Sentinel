node_modules
dist
dist-ssr
*.local
.env
.env.*
!.env.example

# Self-hosted basemap payloads are large and node-specific; they are
# provisioned onto the edge node, not committed. Keep manifest.json and
# style.json tracked — they are configuration, not payload.
public/tiles/
public/basemap/glyphs/
public/basemap/sprite*
public/edge-map/data/
public/edge-map/assets/

.DS_Store
.vscode/*
!.vscode/extensions.json
!.vscode/settings.json
*.tsbuildinfo

# Playwright MCP session output (snapshots, console logs, screenshots)
.playwright-mcp/

# Cesium static runtime — reproducible from node_modules, not source.
public/cesium/
