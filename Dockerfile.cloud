FROM node:22-bookworm-slim AS frontend-build

WORKDIR /build/frontend
COPY frontend/package.json frontend/package-lock.json ./
RUN npm ci
COPY frontend /build/frontend
COPY contracts /build/contracts
ENV VITE_TACTICAL_PROVIDER=maptiler
ENV VITE_TACTICAL_STYLE_URL=https://tiles.openfreemap.org/styles/liberty
RUN npm run build

FROM python:3.12-slim

WORKDIR /app

COPY backend/requirements.txt /app/backend/requirements.txt
RUN pip install --no-cache-dir -r /app/backend/requirements.txt

COPY backend/app /app/backend/app
COPY contracts /app/contracts
COPY backend/hosting.py /app/backend/hosting.py
COPY --from=frontend-build /build/frontend/dist /app/frontend/dist

ENV PYTHONPATH=/app/backend
EXPOSE 10000

CMD ["sh", "-c", "uvicorn hosting:app --host 0.0.0.0 --port ${PORT:-10000}"]
