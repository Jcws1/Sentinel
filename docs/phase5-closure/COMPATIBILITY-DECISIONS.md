# C01–C23 sign-off table

**Awaiting user sign-off.** No C-item behavior changed in this closure except
the C17 numeric-basis supplement (below), approved by the user for
implementation and recorded here for sign-off. Organiser conformance
remains **unclaimed**: no organiser answers are on record. Recommendations:
**Accept** = adopt as final local policy; **Change** = alter behavior (none
recommended); **Open** = keep provisional until the organiser rules.

Source register: [compatibility-decisions.md](../../contracts/simulation/compatibility-decisions.md);
approved supplements: [Phase 5 DECISIONS](../phase5-simulation-compatibility/DECISIONS.md).
Test names are in `backend/tests/` unless a path is given; "corpus" is
`scripts/simulation_system_corpus.py`.

| ID | Ambiguity | Current behavior | Covering tests | Risk if organiser rules otherwise | Recommendation |
| --- | --- | --- | --- | --- | --- |
| C01 | §7 prose says probabilities 1/0; JSON has one I→I rule at 1 and a mutual golden | JSON and golden response used verbatim; both directions apply | `test_frozen_golden_exact`, service golden test, corpus golden, `verify_phase0.py` | Low: golden output is itself published | **Accept** |
| C02 | Aborted RESUME matches both RUN_NOT_HELD and RUN_TERMINAL | Specific RESUME row wins: 409 RUN_NOT_HELD | `test_lifecycle_confirmed_and_provisional_matrix`, §9 lifecycle test, corpus lifecycle | One error code in one cell differs | **Open** (ask organiser) |
| C03 | Lifecycle matrix omits cells (no-run HOLD/ABORT, held START/HOLD, failed row) | Complete provisional matrix; unspecified cells 409 with named codes; held HOLD is a recorded no-op | Lifecycle matrix test (16 cells), corpus lifecycle (16 cells) | Missing cells may expect different codes or a success | **Accept** matrix as local policy; failed-row semantics **Open** |
| C04 | Organiser invocation surface (HTTP service vs function) | `POST /api/simulation/v1/commands` plus pure `resolve_drone_attack_simulation` | Service and resolver suites, corpus | Integration adapter needed for another surface | **Open** |
| C05 | Required envelope cannot be filled when identity is unreadable | Minimal `{error:{code,path,message}}` only then; full envelope otherwise | `test_unrecoverable_identity_has_documented_minimal_failure`, corpus envelope checks | Error body shape change | **Open** |
| C06 | Request null ban vs nullable response errors | All request nulls rejected; success responses carry null error fields | `invalid-null` fixture, golden | Low | **Accept** |
| C07 | Canonical encoding; array permutation vs idempotency | Recursive key sort, numeric lexical normalization, array order retained for identity. Observation: reordering a profile's rules under the same identity is a 409 CALIBRATION_ID_CONFLICT (with C15) | `test_numeric_canonicalization_and_array_identity`, `test_retry_returns_exact_stored_response_after_abort_and_numeric_variants`, `test_reordered_rules_under_one_profile_identity_are_a_calibration_conflict` | RFC 8785 or order-insensitive identity would change idempotency/conflict outcomes | **Open** |
| C08 | "First error in deterministic document order" undefined | §4.1 field order, then timestamp order and array index; JSON Pointer paths | `test_document_order_before_unknown_keys`, `test_frozen_invalid_fixtures` and the corpus (each frozen negative pinned by code and path) | Different error path or precedence | **Accept** as local policy |
| C09 | Duplicate JSON keys / duplicate parsed timestamps | 400 DUPLICATE_KEY before schema validation | `test_strict_parser`, `duplicate-key` fixture, corpus duplicate timestamp | Organiser may expect 422 | **Open** (HTTP code) |
| C10 | Rule coverage "for pairs that can occur" | Supplement (approved): rules required for eligible pairs across all samples; HOLD/ABORT validate but need no coverage; duplicates invalid | `missing-rule` fixture (MISSING_RULE), `test_control_validates_but_does_not_resolve_or_require_rule_coverage`, corpus duplicate rule | Stricter or looser coverage rule | **Accept** (approved supplement) |
| C11 | Zero-health wording vs REMOVED nonparticipants | DISABLED/REMOVED nonparticipants preserved; only participating ACTIVE drones become DISABLED at zero | `test_neutral_unknown_outside_and_terminal_rows_preserved`, `neutral-removed` fixture, §9 unchanged-rows test | Terminal status transitions differ | **Accept** |
| C12 | External interaction hash excludes command/run; pipes allowed in IDs | External hash preserved verbatim; internal identity is structured | `test_pipe_delimited_hash_collisions_do_not_drop_interactions`, `test_large_hash_collision_keeps_distinct_pairs_in_recorded_events` | None locally; the hash is the organiser's | **Accept** |
| C13 | Run identity; command ID scope | One run per mission; command IDs global | Conflict and lifecycle tests, `test_external_id_namespace_cannot_overwrite_interactive_or_fixture_identity` | Mission-scoped IDs would change conflict outcomes | **Open** |
| C14 | Backward/overlapping samples and source changes across commands | Accepted and recorded; latest recorded sequence resolves equal effective time | `test_backwards_resume_missing_observations_and_command_scoped_events`, UI RESUME in REPLAY mode | Organiser may reject overlaps or source changes | **Open** |
| C15 | Profile changes across commands | A different immutable profile per command is allowed; same identity must be identical | `test_profile_immutable_and_nonnotional_claim_requires_external_artifact`, reordered-rules conflict test | Run-frozen profile would reject some sequences | **Open** |
| C16 | Discontinuity after absence/RESUME | Compare with last observed calculated output in the run; first observation false | `test_discontinuity_compares_last_seen_output_after_absence`, §9 RESUME correction test | Previous-snapshot comparison would flag differently | **Open** |
| C17 | Polygon interior at poles/dateline; degenerate rings; numeric basis | Planar non-wrapping lon/lat, inclusive edges, zero-area rejected (approved). **New supplement (approved by the user for implementation on 2026-09-24, recorded for sign-off):** topology and containment are exact on each coordinate's shortest round-trip decimal, one predicate for validator, core, frontend and resolver. It is stricter than the previous inexact arithmetic in one direction: genuinely self-intersecting mixed-exponent rings that the old validators accepted are now rejected (the round-2 critic built such rings); a recording or completed command made before the upgrade with such an area would then fail strict reads and identical retries. The tracked data and both operator databases hold none | `test_geometry_exact.py`, `test_geometry_history.py`, §9 boundary tests, corpus geometry/containment groups, browser R3-1/spike tests, `frontend/tests/unit/exactGeometry.test.ts` (shared vectors include polygons with up to four holes in every order); approved read-only operator scans | Binary-float or tolerance-based semantics would change only sub-ulp boundary cases; wrapping interiors would change dateline/pole areas. Frontend parity relies on the engine choosing the closest shortest digits, which ECMAScript recommends; verified for Node 24/V8 (Edge's engine family) on 1.5 M doubles by the round-1 critic and 343,038 by the round-2 critic | **Accept** numeric basis and planar policy; global interiors **Open** |
| C18 | Draw division and rounding ties | Exact integer division to nearest double; separation rounded to 3 decimals (Python round, ties-to-even) | `test_draw_double_can_round_to_one`, golden | Tie rounding could differ in rare separations | **Open** (tie rule) |
| C19 | `execute_at` as schedule vs lower bound | Lower bound only; synchronous evaluation | `before-execute` fixture | Organiser may expect deferred execution | **Accept** as local policy |
| C20 | Rejected envelope FAILED vs existing run | Rejections leave the committed run unchanged | Lifecycle 409 state checks, §9 LIVE rejection test | Low | **Accept** |
| C21 | drone_id trimming | Padded drone IDs permitted (literal text); other IDs trimmed | `verify_phase0.py` padding checks | Organiser may intend trimming | **Accept** literal text |
| C22 | Leap seconds, calendar dates | Supplement (approved): second 60 rejected; calendar validated | `test_calendar_and_approved_leap_second_policy` | Leap-second acceptance would change validity | **Accept** (approved supplement) |
| C23 | Runtime/memory at contract maxima | Contract bounds unchanged. Current measured worst update gaps: 10k/one timestamp 7.0 s; 40×100 timestamps 8.9 s; 300 growing-world timestamps 16.2 s. Sparse 10k and dense 50–200 processing/memory measured. Largest tested persisted UI body 2,283,322 bytes / 9,687 one-timestamp observations; no portable maximum or contract-maxima throughput guarantee | [Assessment performance](../assessment/PERFORMANCE-AND-LIMITS.md), corpus 10,000-observation snapshot; [historical performance](PERFORMANCE.md) | Organiser may need an envelope covering timestamps, retained state, geometry, output density and browser storage as well as units | **Open**; P5-BATCH remains a known limitation, with formal acceptance pending |

**Lifecycle matrix (C02/C03).** Idempotency precedes transitions; a completed
identical request returns its stored bytes even after ABORT. All 16 non-failed
cells are exercised over HTTP by the corpus and by the service matrix test; the
FAILED row is not reachable through the API and stays provisional.
