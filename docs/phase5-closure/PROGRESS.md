# Progress ledger

Short dated milestones so work can resume after context loss. Times are UTC.
Raw outputs live under ignored `test-results/phase5-closure/`.

## Assessment handoff — 25 September 2026 Singapore time

The subsequent assessment-preparation task preserved and verified candidate 6,
then added application diagnostics, a separate saved-plan review launcher and
neutral varied inputs with tests. The current working tree is **assessment-ready**,
digest `0207f2e9a30614cae0bdcbfe10cd3907305ba06d3c58ffde013258960ee154e5`
(680 non-documentation files), HEAD `f98de4f` plus uncommitted changes.
Start with [assessment results](../assessment/TEST-RESULTS.md),
[implementation notes](../assessment/IMPLEMENTATION-NOTES.md) and
[the review guide](../assessment/START-HERE.md). Raw evidence is in
`test-results/assessment/`, plus the named simulation benchmark/corpus folders.

Backend 768, frontend 637, browser 132 and system 387 checks pass on this source,
with 26 native foreground ownership checks. The runner's initial temporary-parent
setup failure and corrected complete backend reruns are retained. Current
measurements include the timestamp dimension (worst gap 16.2 s), browser storage
and exceptional geometry costs. The five copied saved plans all validate; the
production-launch rehearsal printed real logs and matched HTTP trace IDs.

**Do not resume by assuming the working tree still has candidate 6's digest.**
Its verified source archive remains in `test-results/assessment/baseline/`.
The old candidate-5 report was also preserved in `gate-c5/TEST-REPORT.md`.
Formal independent critic round 3, its acceptance decision and C01–C23 sign-off
remain outstanding. Author-run foreground tests do not replace the independent
UI critic. Review any later code changes against the new frozen source before
using this evidence; no commit or push was performed.

## 2026-09-24

- **00:10–00:38 Baseline complete.** Clean HEAD `f98de4f`; 437 database artifacts
  inventoried without opening; backend 562/562; frontend 485/486 (pre-existing
  Cesium import timeout); static checks pass; optional browser 126/126. Real API
  reproduces R3-1 and a new decimal-view spike as HTTP 500 and a wrong
  containment result. The optional browser suite (00:23–00:38) overlapped the
  first edits but served bundles built from HEAD; see [BASELINE](BASELINE.md).
- **00:31–00:45 Geometry implementation (candidate not yet frozen).**
  - New shared predicate: `backend/app/domain/geometry.py` (exact integers on
    decimal spellings) and `frontend/src/contracts/exactGeometry.ts` (float filter
    + BigInt fallback). Wired into `domain/models.py` `Polygon`,
    `simulation/validation.py` (`validate_ring`, `inside_polygon`) and
    `simulation/resolver.py`; `contracts/integrity.ts` uses the TS module.
  - Shared vectors `frontend/tests/fixtures/geometry/polygon-vectors.v1.json`
    (64 rings, 7 hole cases, 16 containment points), generated by
    `backend/tests/geometry_vectors.py` from the independent oracle
    `backend/tests/geometry_oracle.py`.
  - Tests: `backend/tests/test_geometry_exact.py` (95 pass),
    `frontend/tests/unit/exactGeometry.test.ts` (80 pass). Focused existing
    simulation/domain/contract/boundary/location files: 198 pass.
  - [PLAN](PLAN.md) budgets written before any measurement.

- **00:45–01:36 Remaining implementation (pre-freeze).**
  - Historical compatibility: tracked data and the approved operator-database
    scan (57,105 frames, 22 revisions; 21 distinct polygons in the operator
    database and 1 in the review copy) show zero verdict changes; originals
    unchanged, scratch copy deleted.
  - §9 dataset (two synthetic areas, START/HOLD/RESUME/ABORT) with 14 backend
    criteria tests, browser test and foreground flow; replay deferred to Phase 7.
  - System corpus `scripts/simulation_system_corpus.py`: dev run 386 cases,
    0 failures, 0 server errors (first dev attempt had 3 harness mistakes, kept).
  - Concurrency harness + opt-in probe runtime; decode/validation A/B tools;
    storage-limit probe (first attempt raced the async file read; fixed).
  - Defects fixed: exact geometry (P5-GEOMETRY), resolver containment near long
    edges, raw `QuotaExceededError` text shown to operators, UTF-8 portability
    (probe states the historical cp1252 decoding; no frozen value changed).
  - Foreground harness now verifies the OS foreground PID before every CDP
    screenshot; dev smoke passed 7 cases and 20/20 PID checks.
  - Sanity (not a gate): backend 675/675, frontend 567/567, all prerequisites.

- **01:36–02:25 Candidate 1 gate (test runner).** Frozen at 01:36:09 by inventory
  digest under `test-results/phase5-closure/candidates/c1/`; runner window
  01:38–02:20. G2–G7 passed; G1 failed
  only on `format:check` because a post-freeze edit of `frontend/tests/README.md`
  was written with CRLF (a documentation file; fixed by rewriting LF bytes; the
  failed attempt is retained). M1, M3, M4, M5 and M7 within budget (M5 worst
  1.089 against 1.10); M2 disclosed with no loss or reorder. See
  [TEST-REPORT](TEST-REPORT.md). The runner's step labels G0–G7 are its own
  step numbers, not the hard gates G1–G11 of [PLAN](PLAN.md).
- **02:25–02:28 Candidate 2 changes (before critic round 1).**
  - `geometry.py` performance: streamlined decimal parsing, inline scaling and a
    single-ring fast path (an ad-hoc, unretained timeit put a 5-vertex ring at
    17.2 µs against the legacy 14.3 µs; only gate measurements are evidence).
    No verdict changed; 265 related backend tests pass.
  - `SimulationPane.tsx`: the "Open complete recorded JSON" link collided with
    the "Source timestamp" label in the recorded inspection (seen in the gate
    screenshots); it now sits in the existing `simulation-actions` row.

- **02:30–02:40 Candidates 2 and 3.** Candidate 2 frozen at 02:33:52 (digest
  `ace42f7e…`). While writing [PERFORMANCE](PERFORMANCE.md) I found that the
  concurrency harness timed only the `renew` command request; the preceding
  intent request absorbed the sparse-10k stall (renew loop paused 6.6–6.7 s,
  reported maximum 446 ms). The harness now times the whole control action and
  reports both parts (smoke: control 219 ms, sparse 10k 5,944 ms; not
  evidence). Candidate 3 frozen at 02:39:58 (digest `80e17cec…`); it differs
  from candidate 2 only in `scripts/performance_simulation_concurrency.py`.
  Candidate 2 was never gated or reviewed.

- **02:41–04:42 Critic round 1 started, then interrupted.** Both critics were
  launched on candidate 3 at 02:41 and stopped by an API usage limit at about
  03:04 and 03:08, before writing their reports. At 04:42 the candidate digest
  was unchanged, no task port was listening, and only the technical critic's own
  scratch computation was still running; both critics were resumed with their
  context intact. The technical critic finished at 05:10. The UI critic was
  stopped by the limit once more, re-checked (digest unchanged, ports free) and
  resumed at 09:42; it finished at 09:52.

- **09:52–10:40 Round 1 results and corrections (candidate 4 content).**
  Technical critic 8.8/10 (M1 tautological lifecycle oracle, six Lows); UI/UX
  critic 7.2/10 (H1 single-press ABORT, eight Mediums, thirteen Lows). Every
  finding answered in [REVIEW-RESPONSES](REVIEW-RESPONSES.md). Corrections: literal
  §8 matrix (the critic's mutation plugin now fails three cells); exact
  bounding-box early exit in both predicates; §9 apex vertex pair, separate
  clean-run processes and reversal assertion; TypeScript reason order; ABORT
  confirmation, focus-preserving actions, truthful stored-result/outage/rejection
  wording, per-timestamp health changes, finalized-mission indicators, accessible
  names. A dev corpus run exposed a hard-kill race in the harness (failed run
  kept); the kill now triggers on the durable pending state. Sanity (not a gate):
  backend 680/680, frontend 578/578, Simulation and analytics browser specs
  10/10, corpus 386/0/0 three times, foreground smoke 7 cases with 20/20 PID
  checks.

- **10:30–12:13 Candidate 4 gate, then candidate 5.** Candidate 4 frozen at
  10:30:03 (digest `4787b0d6…`, 28 files changed from candidate 3). The approved
  read-only operator-database scan on it: 0 verdict changes (57,105 frames, 22
  revisions, 21 distinct polygons in the operator database and 1 in the review
  copy); neither operator database holds stored
  external commands; originals unchanged, copy deleted. The runner's gate
  (interrupted ≈41 min by an API usage limit, resumed; candidate untouched)
  passed every step except M7, which was INCOMPLETE: the storage probe waited
  30 s for the Submit button while the page was still rendering a 16,000-drone
  draft. Diagnosis: the candidate 1 and candidate 4 builds take the same time
  for that draft (33 s and 36 s until it registers), so this is a race in the probe, not a
  regression. A lead variant with a page-settle wait measured candidate 4's build
  at 9,687 drones persisted (as candidate 1). Candidate 5 (frozen 12:12:57,
  digest `27758cb4…`) fixes the tracked probe (settle wait, no timeout changed)
  and states when a draft is not a readable v1 request; its affected checks
  passed (Simulation spec 6/6; probe complete, 9,687 drones).

- **12:20–13:06 Candidate 5 final gate: every step passed.** Prerequisites 10/10;
  frontend 579/579; backend 680/680 (and with `PYTHONUTF8=1`); corpus 386/0/0
  (hard kill on the durable pending state); browser 129/129 with no skip, flaky
  or retry; foreground 0/10/20 with 24/24 PID matches (one transient foreground
  wait on the workload-20 run resolved itself); M1 239/260/286 ms (≤ 750); M2
  disclosed, no loss or reorder; M3 within goals; M4 worst 1.012; M5 worst 1.065;
  M7 9,687 drones persisted, §9 largest body 2.0%. Status documents updated with
  the decision pending critic round 2.

- **13:16–14:42 Critic round 2 on candidate 5.** Technical critic 8.8/10 (1 Medium:
  P5-BATCH measured only on one- and two-timestamp batches; 5 Low). UI/UX critic
  8.5/10 (3 Medium: finalized state truncated in the header, editor ABORT
  unconfirmed, stale recorded-command list; 14 Low); its independent foreground
  verification passed on candidate 5 (107/107 native PID matches). Both below
  9.0, so round 3 (the last allowed) is required. Dispositions:
  [REVIEW-RESPONSES](REVIEW-RESPONSES.md), "Round 2".

- **14:45–15:28 Candidate 6 corrections, dev checks and freeze.** All four
  Mediums and most Lows corrected (21 non-documentation files changed from
  candidate 5); M9 (timestamp dimension) and the accordion/containment
  diagnostics predeclared in [PLAN](PLAN.md) before any measurement. Dev
  checks, not gate evidence (`test-results/phase5-closure/dev/presanity-c6/`):
  frontend 637/637, backend 733/733, Simulation spec 7/7, complete browser
  suite 130/130 (0 skipped, flaky or unexpected), corpus 387/0/0, foreground
  workload 0 with 22/22 PID matches (finalized tag whole at 760 and 1440 px),
  the round-2 critic's 3,000 multi-hole polygons with 0 reason differences,
  and a one-run dev M9: `steps100x40` paused interactive delivery about 8.1 s
  (batch 8.9 s, 4,000 rows) and `churn300` about 15.4 s (batch 15.4 s, 300
  rows), with no loss, reorder or resync. A two-round M4/M5 smoke showed
  every frame accepted by HEAD and candidate; box-dense and mixed-exponent
  diagnostics cost more than HEAD (for example resolver containment on a
  101-position mixed-exponent area about 11× HEAD). Candidate 6 frozen at
  15:28:06 UTC: digest
  `bd24e1688cce88b51ae9a12a9e0f7197b900dec47d2e377459d4629827744a5b`
  (668 files, `test-results/phase5-closure/candidates/c6/`).

- **15:35 Safe stop at the user's request.** Nothing running: task ports free,
  no task processes, the 437 original database artifacts unchanged and no task
  database left, `frontend/test-results` empty. Candidate 6 has **not** been
  gated or reviewed. Handoff material (the original brief, the candidate 6
  runner brief, round-2 critic briefs as round-3 templates, close-out tools and
  plan, patch scripts) is in `test-results/phase5-closure/handoff/`.

## Historical next steps from the candidate-6 pause

This original handoff is retained for context. The assessment handoff above
supersedes its source identity and gate status; the independent review and
decision requirements still apply to the actual final source.

1. Confirm candidate 6 is unchanged: `inventory.py` digest must equal
   `bd24e168…744a5b`. If any non-documentation file changed, freeze a new
   candidate and gate that instead.
2. Copy this `TEST-REPORT.md` (candidate 5) to
   `test-results/phase5-closure/gate-c5/TEST-REPORT.md`, then have the test
   runner execute `handoff/runner-brief-c6.md` (complete gate including M9)
   and write the new TEST-REPORT for candidate 6.
3. Update the status and closure documents with candidate 6's gate numbers:
   PERFORMANCE (M1–M9, accordion and containment diagnostics), the C23 row,
   README "Current limits" (timestamp dimension and the UI batch-size limit),
   architecture §5/§11 (80-row wording, one C17 wording, P5-GEOMETRY status,
   P5-BATCH row, the missing comma in the §10 measurement links),
   IMPLEMENTATION_PLAN top entry and §10 paragraph, the closure README gate
   table and single-operator note, TRACEABILITY status key.
4. Critic round 3 (the last): fresh technical and UI/UX critics on candidate 6
   (templates `handoff/critic-*-r2.md`); the UI critic must record native PID
   foreground matches (G9).
5. Responses; decision (accepted only if both critics are at 9.0 or above with
   no unresolved Critical, High or Medium finding and every gate passes;
   otherwise acceptance withheld); `docs/ARCHIVE.md` entry; close-out in order:
   archive with readback, stop services, cleanup of task-owned outputs,
   preservation comparison, seal (`handoff/closeout-plan.md`,
   `handoff/closeout-tools/`); final report. No commit or push.
