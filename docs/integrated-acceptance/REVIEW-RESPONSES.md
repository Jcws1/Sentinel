# D7 implementation responses

## Round 1

The [independent round-1 report](critic-round-1.md) scored its reviewed pre-correction state **8.8/10** and held acceptance. Its scores do not certify later edits.

| Finding or challenge | Response and verification |
| --- | --- |
| LOW: ended recorded Fleet advises acquiring/reclaiming control | Accepted. Terminal authoritative frames now explain that recorded inspection is read-only before offering lease guidance. Controls remain refused. Two focused cases check both `ownsControl=false` and nominally true; direct Move, script control and behavior capture all reject. All403 frontend cases pass. Fresh round2 checks final UI/source. |
| Camera readback oracle and complete browser gate | Accepted. Both failed complete runs are retained. Only the derived Cesium pitch allows the same64-epsilon numerical bound; Tactical and other camera fields remain exact. The shared assertion covers the remaining affected3D readback paths. A complete final browser run is mandatory. |
| Performance/resource claims must be limited to measured evidence | Accepted. Compositor intervals/stalls are separate from RAF counts; emulated4K is not physical4K; DOM listeners are not a React-subscriber census; payload characters are not exact wire bytes. The recorded757.65MB history growth and prior configured Video limit remain explicit. |
| Independent recorded screenshot initially preceded populated renderer | The critic captured a separate successful supplement with40 displayed IDs, exact ended sequence and frozen Sydney geometry. Earlier screenshot and runner failures remain retained. |

The critic's own initial failures were verification-program assumptions, documented with evidence: missing optional Playwright FFmpeg, a read before committed End, omitted rather than null optional `activeMissionId`, and Review before committed Pause. It corrected synchronization/contract expectations without changing application code or using blanket retries.

## Round 2

The [fresh round-2 report](critic-round-2.md) scores the corrected change **9.0/10**: frontend/UI 9.4, backend/simulation 9.3, compatibility/recovery 9.4, performance/resources 8.0, maintainability 9.0. It recommends scoped functional/recovery acceptance and withholds performance and unqualified overall acceptance. This score replaces round 1 for the final source; it does not certify the whole application.

| Finding or challenge | Response and verification |
| --- | --- |
| LOW: measurement caller retained a duplicate premature validation handshake; intermediate helper read the wrong receipt level | Accepted. Both setup paths now call `validateSavedScenario`, which verifies the accepted Save receipt's `result`, complete validation response, exact definition/revision/hash, and matching visible review. No blanket retry, sleep or assertion-timeout increase. Subsequent configured setup passed; its 5.507-second visible-readiness time is reported. Both zero-provider failed attempts are retained. |
| Verify ended-recording advice on corrected source, not old screenshots | The critic personally reopened the recording, observed all 40 rendered IDs at the exact End sequence, and verified read-only explanations plus disabled Move/Apply/Stop/Return controls. Its own screenshot is indexed in the evidence report. |
| Require one complete final browser gate | `browser-full-3` passed all 117 cases in one 879.058-second run with no skipped/flaky/retried cases. The critic independently inspected both JSON and text results. |
| Separate loaded provider content from measured pacing | Accepted. Google Video passed visible-content readiness; continuing requests triggered the cap during capture. No complete Video FPS sample or overall performance acceptance is claimed. Prior limitations, recording growth and uneven frame tails remain explicit. |

Round 2 personally passed 105 frontend and 127 backend tests and a complete isolated foreground remote 40-unit workflow, including uncertainty/reload/restart/recorded inspection. Its final inventory covers 320 production/test files and matches delivery. All 437 original database artifacts retained their size/mtime. Its services, browser context and task database were cleaned up. No material fix followed this final review; remaining delivery edits are documentation and ignored archive/cleanup programs.
