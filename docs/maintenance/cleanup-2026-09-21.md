# Repository cleanup — 21 September 2026

Status: implementation and verification complete; independent scoped acceptance
9.4/10. Final delivery is governed by the archive/readback and disposal receipts
listed below. This maintenance task does not close
Phase 5, Phase 6 or D7 acceptance gaps and does not start another roadmap phase.

## Baseline and plan

The actual dirty working tree is the baseline, at HEAD
`30414540b89508193ea84c4f8e235362ebd61a37`. The initial inventory contains 790
source files, 437 existing database artifacts, three environment files and 857
files in the protected research/public-asset trees. Its SHA-256 is
`9e771bf42c0cd71192e74e982dd175fdf2c19c8bd7319bbc897fa30357dec3b6`.
No Sentinel processes or listeners were found on the inspected application/test
ports. The baseline source copy, diff, status, database metadata, environment
hashes and protected-tree hashes are in the task evidence.

1. Trace production modules, named exports, package dependencies, dynamic imports,
   registries, scripts, fixtures and ignored outputs.
2. Remove only proved unused declarations; retain uncertain compatibility models.
3. Archive obsolete review programs and duplicate test copies with a per-file
   move map and complete hash/readback before original removal.
4. Verify complete frontend/backend, static/schema/foundation/build and browser
   gates. Compare matched production output before/after the source cleanup.
5. Obtain fresh independent source/archive and actual foreground review.
6. Archive final evidence, verify preservation, stop owned services and remove
   disposable task outputs. No commit or push; documentation rewrite is separate.

## Candidate decisions

| Candidate | Evidence / consumers | Decision / verification |
| --- | --- | --- |
| `backend/app/commands/unit_profiles.py::placement_speed` | No caller/import in the AST audit of application, tests or scripts, and no name-based loader/reference. Active scheduler uses `entity_speed`; scenario analysis/service use `profile`. | Remove unused helper only; backend suite and frozen contract checks. |
| `frontend/src/world/scriptPlan.ts::scriptStep` | TypeScript symbol audit finds only its declaration. Active nominal movement uses `placementSpeed(unit) * 0.2`; no namespace/dynamic consumer of this export. | Remove obsolete fixed-speed constant; complete unit/browser gates and matched build-byte comparison. |
| Legacy intent/export aggregate models | Static zero-use candidates exist, but their modules contain active historical readers and retain version-specific schema representations. | Retain; no broad compatibility-model pruning on static evidence alone. |
| Entire production modules | Import/export audit, dynamic renderer imports and view registration did not justify a whole-module removal. Static absence alone would not establish safety. | Retain modules; do not infer every member is indispensable. |
| npm/Python dependencies | In particular, TanStack is used by TracksBrowser and Axe by canonical browser accessibility tests, despite no use in some other panes. | Retain; no dependency change is justified by initial candidates. |
| Frozen contracts, foundation drafts and fixtures | Current export/foundation scripts and historical recording validation consume them. | Retain byte-identically. |
| Supplied photographs, public assets and renderer workers | Details uses explicit portrait mappings; App references the logo, WorkspaceHost configures `popout.html`, and SDK workers/assets are loaded by runtime URLs. Static import absence would be misleading. | Preserve all assets; no phase expansion or new pop-out behavior. |
| Similar historical/current validators and model fields | Versioned contracts and recording readers have different compatibility responsibilities. No equivalent ownership/behavior proof justified consolidation. | Retain; no abstraction or semantic rewrite. |
| Ignored presentation-test copies/configurations and one-off review scripts | Point to old task outputs; current testDir, package/build entries and support imports use tracked canonical tests. | Audit individual paths, archive before removal; preserve tests and old results. |
| Old standard generated test builds | No serving process found; current source has advanced beyond their generated bundles. Both test builds passed a scan for configured credential values. | Archive `dist-test` and `dist-verification`; tests rebuild isolated `-cleanup` outputs. Canonical public assets remain unchanged. |
| Configured `frontend/dist` | An existing configured provider value occurs in the generated bundle. | Retain in place; do not copy configured credentials into the archive. |
| Four tracked `frontend/docs/**/browser-results.json` reports | No active reference outside their own historical paths; current canonical reporter uses `test-results/browser`. | Archive with original paths/hashes. Add a narrowly scoped ignore rule against regenerating these raw reports in source. |
| Databases, installed dependencies, map pack and local environment | Existing operator/developer data and active setup. | Preserve; do not sweep cache directories containing databases. |
| `research-brain`, `output/documents`, `tmp/report-*`, `tmp/sentinel-*` | Unrelated research and document work, including drafts and final deliverables. | Preserve in place; cleanup authorization does not make them disposable. |
| Historical phase reports | Required provenance and unresolved findings; old “not implemented” statements describe their own delivery dates. | Retain; current index links distinguish historical reports from present state. |

## Existing acceptance boundaries

Phase 5 reports mixed-scale valid geometry HTTP 500, large-batch blocking,
provisional compatibility interpretations and withheld 8.6/10 acceptance.
Phase 6 candidate 22 reports 562 backend, 486 frontend and 126 browser passes,
but a fresh 8.5/10 critic withholds acceptance for combined-Profile pacing.
D7 strict display and configured Video remain incomplete. These are supplied
historical findings, not new cleanup verification or obligations silently closed
by this task. New results, independent review and preservation receipts follow.

## Evidence location

Working evidence: ignored `test-results/cleanup-2026-09-21/` and
`frontend/test-results/cleanup-2026-09-21/`. The new archive is
`C:/Archive/Coding/Sentinel3-archive/2026-09-21-repository-cleanup/`;
existing archives are not overwritten. The historical move plan has 37 explicit
targets, 2,542 files and 2,164,780,016 bytes. It preserves original paths below
`historical/`, including prior failed results. Moving files on the same volume
does not reclaim disk space.

## Current verification and source identity

The tested product/test/contract-input inventory has 627 files; its SHA-256 is
`a7cfc973e966ba9f1c5ecd408f794bffde56420322320c9b647a7eaf56aecd70`.
Exactly the two declarations listed above differ from the preserved starting
product source. No dependency manifest, lockfile, test or contract changed.

| Gate | Result |
| --- | --- |
| Full backend suite | 562 passed; 89.08 s; exit 0; two existing dependency deprecation warnings |
| Full frontend suite | 486 passed in 49 files; 59.66 s; exit 0 |
| TypeScript / ESLint / Prettier | All exit 0 |
| Frontend current and foundation contracts | Both exit 0 |
| Backend exported contracts | Exit 0 |
| Frozen foundation/schema/specification guards | 43 passed; exit 0 |
| Repository hygiene | Exit 0; final source check and exact change inventory retained with delivery evidence |
| Production and isolated browser builds | Exit 0; existing large-bundle warnings retained |
| Full browser suite | 126/126 passed in 874.751223 s; exit 0; no skipped, unexpected, flaky, retried or errored case |
| Independent focused checks | 39 backend and 45 frontend tests passed; both exit 0 |
| Independent actual foreground review | Complete attempt C passed five workflow groups; 12 screenshots and 12/12 OS PID-matched foreground/visible/focused samples; zero provider attempts, page errors or inspection control commands |
| Task diff whitespace | Exit 0 excluding unchanged unrelated research; baseline research whitespace findings remain |

The complete final browser JSON was parsed, including every case/result and
top-level error. Its SHA-256 is
`d3b5a00fb3f054f0ce6892510485c99cb9148bd8bc40491d140a930e4b862cc0`.
The successful full run is the final-source gate; no subsets, assertion changes,
blanket retries or timeout inflation were used. Historical failed Phase 5 runs
and task-helper setup mistakes remain separately labelled in the archive.

All 403 generated production files (16,423,608 bytes) are byte-identical before
and after this cleanup. The final production and ordinary test bundles are also
byte-identical. The verification bundle remains explicitly separate. The archived
`build-comparison.json` includes every filename/hash; its SHA-256 is
`a0e9e64e71e5684506a8bbd1de07e7a4c0d865a5a152cdd0add9b82018da8a25`.
Builds used matching credential-free environment overrides and shared canonical
public assets; this is not a standalone deployment package measurement.

No performance benchmark was required for these unreachable declarations and
unchanged runtime bytes. This evidence supports absence of an introduced
frontend runtime change, not a new frame-pacing or memory certification. The
Phase 6 and D7 limits above remain open.

## Archive safety

All 2,542 files passed full SHA-256 readback before removal. The move manifest
SHA-256 is `14fb2475fe535b185ec1c4efff929a0bb114690519406a4c92bdc3b380388302`.
The removal pass read both sides again, then rechecked and removed only exact
inventoried files. Empty directories were removed without recursive deletion.
Resolved absolute path and ancestor reparse-point guards apply to both sides.
`historical-readback.json` and `historical-removal-receipt.json` retain the results.

The fresh critic identified a Low task-script race in the earlier proposal to
remove whole directories after a lengthy readback. The exact-file/empty-directory
approach above was implemented and independently accepted before any removal.
No product behavior correction was needed for this finding.

## Foreground and preservation evidence

The critic used the final production build in a fresh Edge context and disposable
database. Their own UI-authoring flow checked the exact 1.6 s dependent preview
and tick-8 acceptance, distinct STING/Hornet speeds and retained altitude. Further
checks exercised Fleet/Details, Command Picture filters, Profile selection,
history, origin and split view, Pause/Resume/End, explicit Previous demos recorded
inspection, and supported external MSL/BLUE/recorded-command inspection after
ABORT. All three probe attempts closed their browsers/services and deleted their
own databases. The complete passing attempt is `critic-ui-c`.

Failed attempt A correctly rejected a sandbox-desktop window (OS hwnd 0).
Attempt B reached real foreground but its probe incorrectly assumed reload
automatically selected the ended recording. Its screenshot showed the supported
Load a mission state. Attempt C explicitly selected the recording through
Previous demos. These were probe/environment corrections; product code and
assertions were not weakened. All attempts, probe copies, hashes and screenshots
remain in the archive. The critic personally inspected all 12 attempt-C
screenshots and the attempt-B failure capture. This bounded
foreground check does not establish 40-unit display performance or close prior
phase gates.

The final preservation comparison finds all 437 original database artifacts
unchanged by size and nanosecond modification time, all three environment files
unchanged by hash/metadata, all 857 protected research/public files unchanged by
hash and all 627 tested product/input files matching their inventory. No new or
missing database artifact or reparse point remains. Of 2,748 original ignored
files retained outside the archive selection, only pytest's regenerated
`backend/.pytest_cache/v/cache/nodeids` modification time changed; its size is
unchanged. Unrelated document drafts/output, `research-brain`, installed
dependencies and configured production output remain in place. This is metadata
evidence for databases and other ignored files, not an unsupported database
byte-equivalence claim.

## Independent acceptance and delivery receipts

The fresh [independent critic](cleanup-2026-09-21-critic.md) accepts the bounded
cleanup: safety 9.6, correctness 9.5, compatibility/regression 9.4,
maintainability 9.3 and overall **9.4/10**. No material cleanup defect remains.
The Low deletion-race finding was corrected and independently rechecked before
any removal. The review distinguishes personally executed checks from supplied
complete suites. Its acceptance does not close Phase 5, Phase 6, external
conformance, inherited D7 or configured Video gaps.

Final source remains at the baseline HEAD. `cleanup-only.diff` compares against
the preserved dirty starting source, not HEAD: two declaration removals, the
narrow ignore rule, two index/archive updates, four archived raw JSON files and
two new maintenance reports. The final source inventory has 788 files;
`final-source-inventory.json` records every size/hash. The 627-file tested
product/input inventory above is unchanged through review and delivery.

The archive retains raw results at their original checkout-relative paths and
the frozen final tree under `source-after/`. In its root:

- `pre-cleanup-manifest.json`, its `.sha256` companion and
  `pre-cleanup-readback-verification.json` record complete readback before
  disposable evidence removal.
- `disposable-removal-plan.json` and `cleanup-summary.json` record exact owned
  output/build files and the separately identified empty historical directories.
  Reproducible task builds are verified against retained build inventories;
  evidence files are verified against their archived copies before removal.
- `final-preservation.json` and `final-source-match.json` check original state
  and the frozen source after disposal. `services-final.json` in the root task
  evidence confirms all known task processes gone and ports
  8011/5181/5182/5451/8251 free.
- `manifest.json`, `manifest.sha256` and `readback-verification.json` seal the
  complete archive, including final receipts. Manifest/readback control files
  describe their own documented inventory scope rather than self-hashing.

All original source/data and unrelated work remain accounted for. No commit,
push, dependency upgrade, storage migration or new roadmap phase is included.
The separate README/architecture documentation task is not performed here.
Static analysis and passing tests do not prove every retained line necessary;
uncertain compatibility candidates remain deliberately documented and retained.

## Archived path ledger

Every destination is `historical/<original path>` under the new archive.
The manifest supplies per-file SHA-256, size and reason; no archive is a runtime
or test dependency. No behavior consolidation was justified.

| Original path | Files | Reason |
| --- | ---: | --- |
| `frontend/docs` | 4 | Four obsolete raw browser-result reports; no active source/test consumer |
| `frontend/.cache/ui-presentation-specs` | 25 | Obsolete task copies of canonical browser tests |
| `frontend/.cache/ui-presentation-partial-specs` | 25 | Obsolete task copies of canonical browser tests |
| `frontend/.cache/ui-presentation-browser-output` | 4 | Historical presentation test output |
| `frontend/.cache/ui-presentation-partial-browser-output` | 1 | Historical presentation test output |
| `frontend/dist-test` | 1184 | Stale credential-free generated test build; canonical public assets preserved |
| `frontend/dist-verification` | 1188 | Stale credential-free generated verification build; canonical public assets preserved |
| `test-results/performance-closure` | 82 | Leftover raw historical measurement output; reusable runners preserved |
| `frontend/.cache/ui-presentation.playwright.config.ts` | 1 | Obsolete task config referencing archived test copies; canonical config unchanged |
| `frontend/.cache/ui-presentation2.playwright.config.ts` | 1 | Obsolete task config referencing archived test copies; canonical config unchanged |
| `frontend/.cache/ui-presentation-r2.playwright.config.ts` | 1 | Obsolete task config referencing archived test copies; canonical config unchanged |
| `frontend/.cache/ui-presentation-partial.playwright.config.ts` | 1 | Obsolete task config referencing archived test copies; canonical config unchanged |
| `.cache/critic-m1-r3-prepare.py` | 1 | One-off historical review/measurement program; canonical workflow does not consume it |
| `.cache/d7-analysis-contention.py` | 1 | One-off historical review/measurement program; canonical workflow does not consume it |
| `.cache/d7-compression-benchmark.py` | 1 | One-off historical review/measurement program; canonical workflow does not consume it |
| `.cache/d7-compression-study.py` | 1 | One-off historical review/measurement program; canonical workflow does not consume it |
| `frontend/.cache/d3a-format-targets.json` | 1 | Historical task target list, log or Synthetic Alpha restart fixture snapshot |
| `frontend/.cache/m12-build-test.log` | 1 | Historical task target list, log or Synthetic Alpha restart fixture snapshot |
| `frontend/.cache/m12-format.log` | 1 | Historical task target list, log or Synthetic Alpha restart fixture snapshot |
| `frontend/.cache/m12-production-build.log` | 1 | Historical task target list, log or Synthetic Alpha restart fixture snapshot |
| `frontend/.cache/restart-before.json` | 1 | Historical task target list, log or Synthetic Alpha restart fixture snapshot |
| `frontend/.cache/restart-before.raw.json` | 1 | Historical task target list, log or Synthetic Alpha restart fixture snapshot |
| `frontend/test-results/.last-run.json` | 1 | Raw previous Phase 5 result; preserve failures and reported exit statuses |
| `frontend/test-results/phase5-browser-focused-attempt1-exit.json` | 1 | Raw previous Phase 5 result; preserve failures and reported exit statuses |
| `frontend/test-results/phase5-browser-focused-attempt1.log` | 1 | Raw previous Phase 5 result; preserve failures and reported exit statuses |
| `frontend/test-results/phase5-browser-focused-attempt2-exit.json` | 1 | Raw previous Phase 5 result; preserve failures and reported exit statuses |
| `frontend/test-results/phase5-browser-focused-attempt2.log` | 1 | Raw previous Phase 5 result; preserve failures and reported exit statuses |
| `frontend/test-results/phase5-browser-focused-attempt3-exit.json` | 1 | Raw previous Phase 5 result; preserve failures and reported exit statuses |
| `frontend/test-results/phase5-browser-focused-attempt3.log` | 1 | Raw previous Phase 5 result; preserve failures and reported exit statuses |
| `frontend/test-results/phase5-build-attempt1.log` | 1 | Raw previous Phase 5 result; preserve failures and reported exit statuses |
| `frontend/test-results/phase5-build-attempt2.log` | 1 | Raw previous Phase 5 result; preserve failures and reported exit statuses |
| `frontend/test-results/phase5-client-attempt1.log` | 1 | Raw previous Phase 5 result; preserve failures and reported exit statuses |
| `frontend/test-results/phase5-frontend-full-attempt1-exit.json` | 1 | Raw previous Phase 5 result; preserve failures and reported exit statuses |
| `frontend/test-results/phase5-frontend-full-attempt1.log` | 1 | Raw previous Phase 5 result; preserve failures and reported exit statuses |
| `frontend/test-results/phase5-lint-attempt2.log` | 1 | Raw previous Phase 5 result; preserve failures and reported exit statuses |
| `frontend/test-results/phase5-lint-attempt3.log` | 1 | Raw previous Phase 5 result; preserve failures and reported exit statuses |
| `frontend/test-results/phase5-typecheck-attempt3.log` | 1 | Raw previous Phase 5 result; preserve failures and reported exit statuses |
