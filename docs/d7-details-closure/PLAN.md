# Milestone 1: D7 and Details closure

Started 2026-09-20 at 07:25:06 UTC. No imposed wall-clock deadline. Stop after
this milestone; no commit/push or Phase 5/6 implementation.

The clean starting tree is HEAD `6c60de5eb40e1e6dd2ba16e4753c90e71adda4d6`.
All 193 production files match the previous performance critic's final inventory.
The baseline snapshot contains 599 source files; 437 original database artifacts
are inventoried without opening them. All four supplied image references exist
and are preserved as raw evidence. Historical timings remain historical until
reproduced with a matched workload.

1. Reproduce and diagnose the 5v0 3D destination refusal on the untouched source.
2. Measure validation/publication, Resume latency and durable recording costs;
   record quantitative goals before each relevant production change.
3. Make justified D7 corrections with exact-content/recovery regressions.
4. Refine only Details using profile-bound supplied images, black framing and
   the reference's image/identity/telemetry hierarchy.
5. Complete matched probes, a before/after ten-minute moving 20v20 soak, actual
   desktop UI/display verification and bounded provider verification.
6. Run complete required checks, obtain fresh independent critic review, correct
   material findings and finish a complete final browser suite.
7. Archive evidence with SHA-256 verification, stop owned services, remove only
   disposable outputs and verify original data/source preservation.

## Measurement and acceptance

- Functional: complete backend/frontend, required static/contracts/guards/build
  and complete final browser suite pass. No weakened negative destination checks.
- Recovery: old/current reader, atomicity, exact retry, restart and recorded
  inspection obligations remain intact; interrupted writes stay unpublished.
- Validation: preserve prior default 40/120 improvement; no >10% repeated median
  regression on matched current-source probes. Active validation must not create
  the previously measured multi-second publication gap. Measure cold separately.
- Recording: investigate the reported Sydney +25% median; repeat controlled
  orders and report all samples. Set storage/CPU goals after profiling, preserving
  every authoritative sample and canonical byte. Compare identical lifecycles.
- Display: uncontended actual-foreground compositor windows; >=60 FPS mean,
  p95 <=16.9 ms, p99 <=33.4 ms, no >50 ms stall. >=100 FPS is a stretch mean.
  Short windows do not replace the ten-minute resource/recording soak.
- Details: correct supplied-image/profile mapping, reference hierarchy, fallback,
  stable telemetry, accessibility and 760/820/900/desktop foreground verification.
- Independent critic: target >=9/10, no unresolved material defect; score cannot
  replace missing measurements or failed gates.

Provider requests are initially zero. The entire pass, including all critics,
shares the existing 4,000 external-request cap with no resets. Every configured
run requires a separately recorded allocation and finite loading/capture window;
stop early enough to account for requests in flight. Blank-grid regression work
uses no provider credentials. An insufficient budget leaves an explicit open gate.

## Starting acceptance matrix

| Area | Status at entry |
| --- | --- |
| Functional | Open: historical complete run 116 pass / 1 fail |
| Recovery | Historical named checks pass; reverify affected boundaries |
| Validation/publication | Prior latency improvement; active-review stall open |
| Storage efficiency | Open; logical bytes unchanged, Sydney timing unresolved |
| Display/Video | Open; historical bounded evidence insufficient |
| Details UI | Generic illustration; refinement authorized |
| Independent review | Fresh review required on final changes |

Raw evidence lives under task-specific ignored result directories before external
archival. Source snapshots, fixtures, resolution/DPR, camera paths, machine state,
request totals and cleanup receipts accompany measurements.
