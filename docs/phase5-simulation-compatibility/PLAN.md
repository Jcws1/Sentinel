# Phase 5: simulation compatibility end to end

Started 20 September 2026 at 10:52:24 UTC. No imposed deadline. Hourly progress
reports use the acceptance checklist, not elapsed time, to estimate work remaining.
Stop after Phase 5; no commit/push or Phase 6 implementation.

Timebox amendment after interruption: resumed 2026-09-20 13:12:32 UTC with a
45-minute hard deadline, 13:57:32 UTC. Reserve 13:52:32 onward for evidence,
shutdown, cleanup and delivery. Prioritize final checks and independent review;
unfinished gates remain explicitly open rather than extending the deadline.

1. Preserve and verify current source/data; freeze explicit and provisional policies.
2. Implement strict complete validation and pure deterministic resolution; prove the
   frozen golden response, negative cases and numerical boundaries.
3. Add durable command/run/profile ownership and adapter publication, with exact
   retry, rollback and restart tests on disposable databases.
4. Add the minimum module-owned batch/lifecycle/inspection workflow, retaining the
   current shell, interactive executor, Details and rendering owners.
5. Measure sparse 10,000-drone and bounded dense work; operate foreground default
   and remote UI, failure/retry, restart, recorded inspection and narrow layouts.
6. Run complete regression/static/contract/build gates; obtain fresh independent
   critic review, fix material findings, and finish a complete browser suite.
7. Archive raw evidence with SHA-256 readback, clean up owned runtimes/outputs,
   audit preservation and report separate acceptance decisions.

No external provider requests are authorized for this phase. All measurements and
critics use isolated databases and fresh local/grid browser contexts. Display
measurements require an exclusive runtime window. Existing D7 pacing failure and
configured Video incompleteness remain open and are not Phase 5 closure claims.

## Measurement plan, declared before implementation

Fixed fixtures: golden two-drone request; default/Sydney 40-drone batches;
10,000-drone spatially sparse single snapshot; dense 50/100/200-drone snapshots.
All profiles remain NOTIONAL. Record request/output bytes, eligible pair/output
counts, complete latency, memory, database/WAL/SHM and normal-close bytes, plus
source publication gaps when an independent forty-unit interactive run is active.
Never omit a qualifying pair or lower contract maxima. Abort a diagnostic run
after 120 seconds or 2 GiB incremental process memory, retain the failure and
report the tested boundary; these harness budgets are not API input limits.

Use three measured runs after one separately reported cold run where practical.
Targets: golden/default forty-unit completion under 1 second warm; sparse 10,000
complete within 30 seconds and the diagnostic memory budget; ordinary forty-unit
external work should keep an independent source's maximum arrival gap <=750 ms.
Large-batch commit/serialization pauses are measured separately. These are local
engineering goals, not contractual or real-time guarantees. Compare unchanged
interactive/storage/validation probes against this actual baseline when affected.

## Initial acceptance matrix

| Gate | Initial status |
| --- | --- |
| Confirmed external contract | Open: structural schemas/fixtures exist; no resolver |
| Provisional local policy | Existing C01-C23 register retained; explicit supplement recorded |
| Functional/UI | Baseline complete unit checks pass; Phase 5 not implemented |
| Recovery/ownership | Existing boundaries verified; new run/journal boundaries pending |
| Performance/resources | Phase 5 probes pending; D7 display/Video gaps inherited |
| Independent review | Source-only contract audit underway; fresh final critic required |
| Evidence/preservation | Source and original database metadata captured before edits |
